require('zx/sets')            -- Sets
require('zx/keymaps')         -- Keympas
require('zx/plugins')         -- Plugins
require('zx/colorscheme')     -- Colorscheme
require('zx/autocmd')         -- Auto Commands
require('zx/lsp')             -- Language Server Protocol

require('zx/autopairs')       -- Auto close: ([{}]) etc..
require('zx/bufferline')      -- Tab Bar on the top
require('zx/cmp')             -- Autocompletion Menu
require('zx/comment')         -- Visual Mode: `gc` & `gb` to comment
require('zx/diffview')        -- Better Diffs, integrates with Neogit
require('zx/gitsigns')        -- :Gitsigns <tab> utilities
require('zx/illuminate')      -- Underline references and definitions 
require('zx/impatient')       -- Cache lua files 
require('zx/indentline')      -- Indentation Line on tabs gaps
require('zx/lspsaga')         -- For better LSP Rename, LSP Finder, Winbar
require('zx/lualine')         -- Bunch of info at the bottom of Neovim
require('zx/neogit')          -- :Neogit, Cool Git Magit-like sh!t
require('zx/nvim-tree')       -- File Explorer
require('zx/project')         -- Rooter and Project Management
require('zx/symbol-outline')  -- LSP Symbol Outline 
require('zx/telescope')       -- File/Words/Literally Anything Finder
require('zx/toggleterm')      -- Manage Neovim Terminal Windows better 
require('zx/treesitter')      -- Cool syntax highlighting & more
require('zx/trouble')         -- :Trouble <tab>
require('zx/which-key')       -- Bottom Menu on pressing leader(space) key

-- TODO:
-- DAP (Debugger Adapter Protocol)
-- Winbar background color (highlight group)
-- Code Folds (https://github.com/kevinhwang91/nvim-ufo)
